- Conduit project from <a src="https://codebase.show/projects/realworld?category=backend&language=typescript">Real World</a> adapted to a different forked backend having a firebase hosted DB and a node server for authentication and user management (set up for local use).

- Set the backend up to avoid CORS errors.
- Changed the backend to send a default image instead of breaking the front-end.
